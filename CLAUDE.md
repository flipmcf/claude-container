Things I want:

I want Claude-container to create Pull Requests.

1. make sure the gh CLI tool is auto-installed when the container is made.
2. create a way to get a deploy key or whatever is necessary to authenticate the gh tool configured into the env.

